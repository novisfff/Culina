import { SummaryStep } from './InventoryReconciliationDialog';

/** Compatibility boundary for the summary step; implementation remains shared during migration. */
export { SummaryStep as InventoryReconciliationSummaryStep };
