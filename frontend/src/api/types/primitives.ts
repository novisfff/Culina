/** Primitive/shared API contracts. Kept type-only to guarantee zero runtime cost. */
export type {
  UserRole,
  FoodType,
  MealType,
  Difficulty,
  InventoryStatus,
  IngredientExpiryMode,
  AiMode,
  MediaSource,
  ImageGenerationMode,
  MediaEntityType,
} from '../types';
